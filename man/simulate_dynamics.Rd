% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_dynamics.R
\name{simulate_dynamics}
\alias{simulate_dynamics}
\title{Simulate the dynamics of all the cases in an experiment
Unfortunately at the moment has features of temperature series hard coded in}
\usage{
simulate_dynamics(
  experiment_folder,
  experiment_design_filename,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{experiment_folder}{The folder where all information about the experiment is stored}

\item{experiment_design_filename}{The filename of the experiment design file}

\item{overwrite}{Logical. If \code{TRUE}, overwrite an existing dynamics database.}

\item{verbose}{Logical. If \code{TRUE}, print progress and output messages.}
}
\value{
Returns nothing. Always saves compact \code{simulation_summaries.RDS} and
\code{population_summaries.RDS}; optionally saves SQLite dynamics/resources
databases depending on output-control settings.
}
\description{
Simulate the dynamics of all the cases in an experiment
Unfortunately at the moment has features of temperature series hard coded in
}
\details{
Experiment JSON files can set \code{model_type} to \code{"lv_discrete"},
\code{"lv_continuous"}, or \code{"consumer_resource_continuous"}. Legacy
\code{dynamics_type} values are still accepted as aliases. Experiment JSON files
can also optionally include
\code{parallel_simulations}, \code{parallel_workers}, and
\code{initial_abundance_seed_base}. They can also include output-control options:
\code{save_dynamics}, \code{save_resources}, \code{dynamics_save_every}, and
\code{resources_save_every}. They can also include \code{summary_checkpoint_every}
and \code{runtime_update_every}. The save interval values are integers giving
the interval between saved output time points. \code{resources_save_every}
defaults to \code{dynamics_save_every}. Compact case and population summaries
are checkpointed by the parent process every \code{summary_checkpoint_every}
completed cases and written again at the end. \code{save_dynamics} and
\code{save_resources} control only the diagnostic SQLite outputs.
\code{save_resources} only applies to
consumer-resource dynamics. When \code{parallel_simulations} evaluates to
\code{TRUE}, simulation cases are computed in parallel and SQLite tables are
still written serially by the parent process. Parallel processing is
intended for macOS/Linux.
}
\examples{
NULL
}
