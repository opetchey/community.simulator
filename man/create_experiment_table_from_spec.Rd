% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_experiment_table_from_spec.R
\name{create_experiment_table_from_spec}
\alias{create_experiment_table_from_spec}
\title{Create a canonical experiment table from a YAML experiment specification}
\usage{
create_experiment_table_from_spec(
  spec,
  output_path = NULL,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{spec}{An experiment specification returned by \code{\link[=read_experiment_spec]{read_experiment_spec()}},
a path to a YAML experiment specification, or a compatible named list.}

\item{output_path}{Optional path to save the resulting table as an RDS file.}

\item{overwrite}{Logical. If \code{TRUE}, overwrite an existing \code{output_path}.}

\item{verbose}{Logical. If \code{TRUE}, print a message when \code{output_path} is
written.}
}
\value{
A tibble with one row per simulation case. The \code{case_spec} list-column
contains the fully resolved nested specification for that case.
}
\description{
Expands a validated YAML experiment specification into one row per simulation
case. Global treatments are applied as overrides to the baseline
specification, LV interaction treatments are expanded as model treatments,
and community/environment replicates are crossed with those treatments.
}
\examples{
template <- system.file(
  "experiment_templates/lv_discrete.yaml",
  package = "community.simulator"
)
if (nzchar(template)) {
  experiment_table <- create_experiment_table_from_spec(template)
}
}
