% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_community_from_spec.R
\name{build_community_from_spec}
\alias{build_community_from_spec}
\alias{build_LV_community_from_spec}
\alias{build_CR_community_from_spec}
\title{Build a community object from a resolved case specification}
\usage{
build_community_from_spec(case_spec, community_seed = NULL)

build_LV_community_from_spec(case_spec, community_seed = NULL)

build_CR_community_from_spec(case_spec, community_seed = NULL)
}
\arguments{
\item{case_spec}{A resolved case specification, usually from the \code{case_spec}
list-column produced by \code{\link[=create_experiment_table_from_spec]{create_experiment_table_from_spec()}}.}

\item{community_seed}{Optional random seed for community construction. When
omitted, \code{case_spec$community$seed} is used, falling back to
\code{case_spec$experiment$random_seed}.}
}
\value{
A model-specific community parameter object.
}
\description{
Builds the model-specific community parameter object for one resolved
\code{case_spec} from the canonical YAML experiment-table workflow.
}
\examples{
template <- system.file(
  "experiment_templates/lv_discrete.yaml",
  package = "community.simulator"
)
if (nzchar(template)) {
  table <- create_experiment_table_from_spec(template)
  community <- build_community_from_spec(table$case_spec[[1]])
}
}
